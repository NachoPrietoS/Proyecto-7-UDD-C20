const GameReducer = (globalState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default GameReducer;